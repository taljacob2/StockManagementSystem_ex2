package application.color;

import javafx.application.Application;
import javafx.stage.Stage;

public class DynamicCSSBackgroundColor extends Application {


    public static void main(String[] args) {
        // launch(args);
    }

    @Override public void start(Stage primaryStage) {
        // // Slider slider = new Slider();
        // // slider.valueProperty().addListener((obs, oldValue, newValue) -> {
        // //     double percentage =
        // //             100.0 * (newValue.doubleValue() - slider.getMin()) /
        // //                     (slider.getMax() - slider.getMin());
        //     slider.setStyle("-track-color: linear-gradient(to right, #90C7E0 " +
        //             percentage + "%, white " + percentage + ("%);"));
        // });
        // StackPane root = new StackPane(slider);
        // Scene scene = new Scene(root);
        // scene.getStylesheets().add("style.css");
        // primaryStage.setScene(scene);
        // primaryStage.show();
    }

}
